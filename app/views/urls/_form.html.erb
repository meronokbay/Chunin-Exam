<%= form_with model: @url, class: 'd-flex gap-3' do |form| %>
  <%= form.text_field :full_url,
    placeholder: 'Type or paste your link',
    "aria-label": 'Full link',
    class: 'form-control',
    required: true %>
  <%= form.submit 'Shorten', class: 'btn btn-primary' %>
<% end %>